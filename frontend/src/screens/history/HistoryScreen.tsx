/**
 * Screen 5 — History & audit lookup (Implementation Companion §C.6).
 * Read-only search by member/date/auth id/code; shows the full decision
 * object AND the rules_version it ran under. Auditor role only for
 * cross-member views.
 */
export function HistoryScreen() {
  return (
    <section>
      <h1>History &amp; audit lookup</h1>
      <p>Not yet implemented — read-only view over auth_decision once decisions exist.</p>
    </section>
  );
}
