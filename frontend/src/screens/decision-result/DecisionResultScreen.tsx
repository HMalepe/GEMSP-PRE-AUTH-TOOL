/**
 * Screen 2 — Decision result + evidence trail (Implementation Companion
 * §C.3). Leads with decision + money; evidence trail collapsible but one
 * click away; caveat always present.
 */
export function DecisionResultScreen() {
  return (
    <section>
      <h1>Decision result</h1>
      <p>Not yet implemented — renders the AuthDecision object once /decisions is real.</p>
    </section>
  );
}
