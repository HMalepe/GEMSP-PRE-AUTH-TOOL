/**
 * Screen 1 — New authorisation request (Implementation Companion §C.2).
 * Single fast form, grouped by the gate each field feeds; keyboard-first,
 * autocomplete against reference data, no free-text code entry. Placeholder
 * until the reference-data autocomplete endpoints and decision API exist.
 */
export function NewRequestScreen() {
  return (
    <section>
      <h1>New authorisation request</h1>
      <p>Not yet implemented — blocked on the decision API (backend §api) and Phase-0 reference data.</p>
    </section>
  );
}
