PORT=3000
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/gems_preauth

# Used only by test/integration/migrations.test.ts — a separate database so
# migration tests never touch dev data. Defaults to gems_preauth_test if unset.
TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/gems_preauth_test
